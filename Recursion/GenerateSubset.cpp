//general subset
//we can generate all subset for length n string using subsets for length n-1 string. 
// ex if for AB subset is " ","b","a","ab".so subset for ABC ,then for that " b"of ab, we can add c(cuz iit is newly added) with b or dont(so,2 possibility(sbset)ie bc or simple b alone)